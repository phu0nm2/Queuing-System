import { Action } from "./../actionsTypes/index";
//
const initialState = {
  //
};

// state, action
const reducer = (state = initialState, action: Action) => {
  switch (action) {
    default:
      break;
  }
};
export default reducer;
